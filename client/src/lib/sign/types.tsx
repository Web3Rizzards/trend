export type PersonType = {
    name: string;
    preferredUsername: string;
};

export type ReactionType = {
    reactionType: string;
};

export type PostType = {
    content: string;
    image: string;
};
