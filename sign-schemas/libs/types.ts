export type PersonType = {
    username: string;
    displayName: string;
};

export type ReactionType = {
    reactionType: string;
};

export type PostType = {
    content: string;
    image: string;
};
